plugins {
    id 'java'
    id 'application'
}



tasks.withType(JavaCompile) {
   //options.compilerArgs << "-Xlint:unchecked" << "-Werror"
   options.compilerArgs << "-Xlint:unchecked"
}

mainClassName = "be.rooi.dijkstra.TestDijkstra"

repositories {
    maven {
        url "http://oss.sonatype.org/content/groups/public/"
    }
    jcenter()
}

dependencies {
}

